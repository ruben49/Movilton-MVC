@model List<movilton_mvc.Models.usuarios>

@{
    Layout = "~/Views/Shared/_Layout_Panel.cshtml";
}
              
<div>
    <ul class="breadcrumb">
        <li>
            <a href="/Home/Panel">Home</a>
        </li>
        <li>
            <a href="/usuarios">Lista de Usuarios</a>
        </li>
    </ul>
</div>

<div class="row">
    <div class="box col-md-12">
        <div class="box-inner">
            <div class="box-header well" data-original-title="">
                <h2><i class="glyphicon glyphicon-user"></i> Usuarios</h2>

                <div class="box-icon">
                    <a href="#" class="btn btn-minimize btn-round btn-default"><i class="glyphicon glyphicon-chevron-up"></i></a>
                    <a href="#" class="btn btn-close btn-round btn-default"><i class="glyphicon glyphicon-remove"></i></a>
                </div>
            </div>
            <div class="box-content" style="display: block;">
                <table class="table table-striped table-bordered responsive">
                    <thead>
                        <tr>
                            <th>Nombre de usuario</th>
                            <th>Email</th>
                            <th>Teléfono</th>
                            <th>Estado</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var lista in Model)
                        {
                            <tr>
                                <td>@lista.nombre_usuario</td>
                                <td class="center">@lista.email</td>
                                <td class="center">@lista.telefono</td>
                                <td class="center">

                                    @if (lista.estado == 0)
                                    {
                                        <span class="label-warning label label-default">Baneado</span>
                                    }
                                    else
                                    {
                                        <span class="label-warning label label-default">Activo</span>
                                    }
                                </td>
                                @{
                            int id = lista.id_user;
                                }


                                <td class="center">
                                    @if (lista.tipo_user == 2)
                                    {

                                    <text>
                                    <a class="btn btn-success" href="~/usuarios/Desbanear?id=@id">
                                        <i class="glyphicon glyphicon-check icon-white"></i>
                                        Desbanear
                                    </a>
                                    <a class="btn btn-warning" href="~/usuarios/Banear?id=@id">
                                        <i class="glyphicon glyphicon-trash icon-white"></i>
                                        Banear
                                    </a>
                                    <a class="btn btn-danger" href="~/usuarios/Desactivar?id=@id">
                                        <i class="glyphicon glyphicon-remove icon-white"></i>
                                        Desactivar
                                    </a>
                                    </text>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>